# ROADMAP

Future intent only. Not part of the contract-doc precedence chain; not handed to coding dispatches.

1. **Periodic load-bearing-ness pruning pass** over definition clauses, across model generations: apply strip-first-observe-patch (README.md, "Variants and platform compatibility") on a cadence, using behavioral probe sets as the instrument. Byte-level checks (`just check`) exist today; behavioral checks do not yet.
2. **Resolve the README ↔ command-files invocation-syntax divergence**: README examples show `KEY=` style argument passing; the command files parse positional/free-form input. Pick one and make the other conform.
3. **Focused kb-orientation pass**: introduce one shared ~12–15-line system-orientation chunk (what the KB system is — two graphs, leaf/summary contract, derived-vs-authored, the cast of agents, a parameterized per-agent role clause), template all 8 kb definitions to include it, and surgically delete each definition's now-redundant scattered restatements. Requires isolated, focused attention — not queue work.
4. **Definition-selection glob for partial renders and installs.** The flavor-vs-pin mismatch itself is settled: gen-defs 1.7.0 resolves each definition's NBs against its *own* frontmatter `model:` pin, and `--model` is now the export flavor for unpinned definitions only — tiering and tuning are coherent per definition, and no render-wide flag can retune a pinned one. Still open: a selection glob (e.g. `--agent-glob="*app-expert*|*-coder*"`) scoping a render or install to an explicitly chosen subset, which has standalone value for partial renders/installs regardless of flavor. Residual footgun, live only if some future mode ever makes frontmatter *follow* a flavor: a glob catching the mad-participant-* set would smooth the debate roster into one model — those four pins ARE the mechanism (independent model perspectives), and a mono-model roster still looks like a working roster while delivering none of the diversity the process exists for. Non-Claude harnesses (opencode etc.) are out of scope for now.
5. No other work intended at this time.
