# What this knowledge base holds

Knowledge base root: `@!dyn.kb-root!@`
Findings on the last attempt: `@!dyn.remediation-source-path!@`

Read the tree under that root and write the passage that opens this knowledge base's overview document: what this body of work argues or proves, what it leaves out, and which of its documents someone arriving with a question opens first.

Where **Findings on the last attempt** carries no path, nothing has been reviewed and this is the first attempt at the question. Where it names a file, an earlier passage was assembled into that document and reviewed, and the file is the review: read it before you write. You are answering the same question again from the tree, not repairing anything — the earlier passage is not in front of you and is not yours to revise. A finding about the passage is answered by a passage that differs where the finding says it was wrong, and by nothing else.

A review covers the whole document, and most of that document is not yours. The counts, the document listing, the headings and the standing instructions about how this KB is read are assembled around your passage from the build's own records. A finding against any of those is not one prose can answer, and rewording your passage toward it makes the passage worse without touching what was reported.

The tree is ground truth. Claim nothing about this corpus you did not read there, and derive nothing about it from anywhere else — not from the source material, not from the subject generally.

Three to six sentences, for a reader who has never seen this corpus and reads your passage before anything else in that document.

State no counts. The document carries its own — documents, claims, nodes, edges, how many claims are scored — taken from the build's records. A number you compose here would be a second copy of one of those, unchecked, in a document whose whole point is that its numbers are exact.

Conduct is not yours either: how this KB is read, how it is changed, what is authored versus derived, and what happens at the gate are stated elsewhere in that document and in the two contract files beside it.

## Returning your work

Your final message body is the passage itself — prose, with nothing before it and nothing after it. Write no file.

No heading, no list, no table, no code fence, no link, no path. The passage lands inside a document that already has its headings and writes its own links, and the only thing that reaches that document is your words. Where you need to point at a document, name it by its title as the tree spells it.
