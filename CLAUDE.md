# CLAUDE.md — agents repo

- Bannered definitions (`# !GENERATED!`) are never hand-edited — change the template or `shared-sections.toml`, then `just generate`.
- `just check` must pass before any handoff touching `templates/`, `agents/`, or `commands/`.
- `agents/` and `commands/` are deployed surfaces — nothing else goes in them.
