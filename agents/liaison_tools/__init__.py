"""liaison_tools — transport/session helpers shared by the liaison agents.

The tools themselves are shell-invoked scripts, not importable modules; this
package marker exists so the test suite under ``tests/`` collects with a
stable module path alongside the ``kb_tools`` suite.
"""
