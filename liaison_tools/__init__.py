"""liaison_tools — transport/session helpers shared by the liaison agents.

The tools are invoked by command line, not imported, and every one of them is
hyphenated so that no name here admits an import. This package marker exists so the test
suite under ``tests/`` collects with a stable module path alongside the
``kb_tools`` suite.
"""
