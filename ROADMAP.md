# ROADMAP — Agent and Command Set Generator

Future intent only. Not part of the contract-doc precedence chain; not handed to coding dispatches.

1. **Periodic load-bearing-ness pruning pass** over definition clauses, across model generations: apply strip-first-observe-patch (README.md, "Variants and platform compatibility") on a cadence, using behavioral probe sets as the instrument. Byte-level checks (`just check`) exist today; behavioral checks do not yet.
2. **Rename the repo to `adjagent`** (settled 2026-08-30; "adjacent agents" — the rendered set installs beside every project it serves). Sequence before the KB_PUB_TODO findability items so search equity lands on the final name. Same stroke: README title, a repo-wide grep for the old name, revisiting the README lede's defensive "rather than collecting them" contrast (the misconception it counters leaves with the old name), and telling the first post-rename session to migrate its path-keyed memory. GitHub auto-redirects the old URL.
