# ROADMAP

Future intent only. Not part of the contract-doc precedence chain; not handed to coding dispatches.

1. **Periodic load-bearing-ness pruning pass** over definition clauses, across model generations: apply strip-first-observe-patch (README.md, "Variants and platform compatibility") on a cadence, using behavioral probe sets as the instrument. Byte-level checks (`just check`) exist today; behavioral checks do not yet.
2. **Focused kb-orientation pass**: introduce one shared ~12–15-line system-orientation chunk (what the KB system is — two graphs, leaf/summary contract, derived-vs-authored, the cast of agents, a parameterized per-agent role clause), template all 8 kb definitions to include it, and surgically delete each definition's now-redundant scattered restatements. Requires isolated, focused attention — not queue work.
